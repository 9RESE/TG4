# =============================================================================
# Database Configuration (REQUIRED)
# =============================================================================
# Generate a secure password with: openssl rand -base64 32
# WARNING: Never commit real passwords to version control!
DB_PASSWORD=
DATABASE_URL=postgresql://trading:YOUR_PASSWORD_HERE@localhost:5432/kraken_data

# =============================================================================
# PgAdmin (optional - web UI for database management)
# =============================================================================
PGADMIN_PASSWORD=

# =============================================================================
# Kraken API (optional - for higher rate limits)
# =============================================================================
# Get your API keys at: https://www.kraken.com/u/security/api
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Generate a secure password: openssl rand -base64 32
# 3. Set DB_PASSWORD and update DATABASE_URL with your password
# 4. Start services: docker-compose up -d
# 5. Verify: docker-compose ps
