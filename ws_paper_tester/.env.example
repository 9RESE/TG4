# WebSocket Paper Tester - Environment Configuration
# Copy to .env and configure before running with historical data

# ===========================================
# Database Configuration (Required for Historical Data)
# ===========================================

# Generate a secure password: openssl rand -base64 32
DB_PASSWORD=your_secure_password_here

# Full database connection URL
# Note: Port 5433 to avoid conflict with local PostgreSQL
DATABASE_URL=postgresql://trading:${DB_PASSWORD}@localhost:5433/kraken_data

# ===========================================
# PgAdmin (Optional - Web UI for database)
# ===========================================

PGADMIN_PASSWORD=admin_password_here

# ===========================================
# Kraken API (Optional - Higher rate limits)
# ===========================================
# API keys enable faster historical data fetching
# Get keys at: https://www.kraken.com/u/security/api

KRAKEN_API_KEY=your_kraken_api_key
KRAKEN_API_SECRET=your_kraken_api_secret
